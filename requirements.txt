# ============================================================
# Exoplanet Detection Pipeline — Requirements
# ============================================================

# --- Astronomical Data Access ---
lightkurve>=2.4.2          # TESS/Kepler light curve download from MAST
astropy>=5.3.4             # FITS I/O, BLS periodogram, units, time
astroquery>=0.4.6          # Alternative MAST/Vizier query interface

# --- Core Scientific Python ---
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.4

# --- Machine Learning (Classical) ---
scikit-learn>=1.3.2        # Random Forest, evaluation metrics, preprocessing

# --- Deep Learning ---
tensorflow>=2.14.0         # CNN classifier via Keras API

# --- Transit Modelling ---
batman-package>=2.4.8      # Transit light curve model (Kreidberg 2015)
transitleastsquares>=1.0.31 # Stellar limb-darkened transit detection (Hippke 2019)
ldtk>=1.7.0                # Limb Darkening Toolkit (Parviainen 2015)

# --- MCMC / Uncertainty ---
emcee>=3.1.4               # Affine-invariant MCMC sampler
corner>=2.2.2              # Corner/posterior plots

# --- Visualization ---
matplotlib>=3.8.0
seaborn>=0.13.0            # Statistical plots

# --- Utilities ---
tqdm>=4.66.1               # Progress bars for batch downloads
requests>=2.31.0           # HTTP requests for MAST API
joblib>=1.3.2              # Parallel processing, model serialization

# --- Jupyter ---
jupyter>=1.0.0
ipykernel>=6.26.0
ipywidgets>=8.1.1          # Interactive widgets in notebooks

# --- Optional lightkurve dependencies (for TPF modeling) ---
oktopus>=0.1.2
autograd>=1.8.0
